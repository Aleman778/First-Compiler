fn main() {
    let a: i32 = factorial(10);
    trace();
    print_int(a);
}


fn factorial(n: i32) -> i32 {
    if n <= 1 {
        trace();
        1
    } else {
        n * factorial(n - 1)
    }
}
