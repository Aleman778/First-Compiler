fn main() {
    let p: bool = is_prime(11);
}

fn is_prime(n: i32) -> bool {
    if n <= 1 {
        return false;
    }
    let i: i32 = 2;
    while i < n {
        if n % i == 0 {
            return false;
        }
        return true;
    }
}
