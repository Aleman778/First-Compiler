fn main() {
    let a: i32 = false;
    let b: bool = sub(a, 3);
    let potatis: bool = !53;
}

fn sub(a: i32, b: i32) -> i32 {
    let c: bool = false;
    {
        let d: i32 = a - b + c;
    }
    return c;
}
